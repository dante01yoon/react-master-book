import React from 'react';
import ReactDOM from 'react-dom/client';
import SyntheticEventExamples from './index';
import './styles.css';

ReactDOM.createRoot(document.getElementById('root')!).render(
  <React.StrictMode>
    <SyntheticEventExamples />
  </React.StrictMode>,
); 