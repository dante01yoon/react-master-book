useEffect(
  setup, // ➊ 필수 인자: 실행할 부수 효과 함수
  dependencies?, // ➋ 선택 인자: 의존성 배열
);


useEffect(() => {
  // 컴포넌트가 렌더링될 때마다 이 코드가 실행됩니다.
});


useEffect(() => {
  // 컴포넌트가 처음 마운트될 때 한 번만 실행됩니다.
  // 예: 최초 데이터 가져오기, 이벤트 리스너 설정 등
  return () => {
    // 컴포넌트가 언마운트될 때 한 번만 실행됩니다.
    // 예: 설정했던 이벤트 리스너 해제
  };
}, []); // 빈 배열은 '의존하는 값이 없음'을 의미


useEffect(() => {
  // userId가 변경될 때마다 실행됩니다.
  fetchData(userId);
}, [userId]); // userId가 변경되면 이 effect를 다시 실행